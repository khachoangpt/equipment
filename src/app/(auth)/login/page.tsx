import LoginTemplate from '@/modules/login/components/templates/LoginTemplate'

export const metadata = {
	title: 'Đăng nhập',
	description: 'Đăng nhập - Quản lý trang bị',
}

const LoginPage = () => {
	return <LoginTemplate />
}

export default LoginPage
